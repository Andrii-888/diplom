import { styled } from "@mui/material/styles";

export const StyledButton = styled("button")(() => ({
  height: "58px",
  width: "100%",
  backgroundColor: "#fff",
  borderRadius: "6px",
  border: "none",
}));
